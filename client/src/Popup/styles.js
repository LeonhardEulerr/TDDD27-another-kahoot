import { makeStyles } from '@material-ui/core';

const useStyles = makeStyles({
  dTitle: {
    height: '25px',
  },
  closeButton: {
    position: 'absolute',
    right: 1,
    top: 5,
  },
});

export { useStyles };
